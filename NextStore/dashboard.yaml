apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  labels:
    app: dashboard-service
  namespace: kube-system
spec:
  type: NodePort
  ports:
    - targetPort: 443
      port: 443
      nodePort: 30443
  selector:
    k8s-app: kubernetes-dashboard 